import { default as auth } from './auth'
import { default as users } from './users'
import { default as folders } from './folders'
import { default as documents } from './documents'
import { default as rules } from './rules'
import { default as docs } from './docs'
import { default as files } from './files'
import { default as transactions } from './transactions'


export default {
  auth, users, folders, documents, rules, docs, files, transactions
}
