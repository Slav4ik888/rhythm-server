// import { mocks } from './mocks';
// import { SCHEMA_NAME } from '../../../../../../../../libs/validators/schemas';
// import { validate } from '../../../../../../../../libs/validators';


// describe(`Validate scheme - ${SCHEMA_NAME.COMPANY}`, () => {
//   mocks.forEach(m => {
//     it(m[0].description, () => {
//       expect(validate(SCHEMA_NAME.COMPANY, m[0].data)).toEqual(m[1])
//     })
//   })
// });

// npm run test:unit validate-company-schema.test.ts
